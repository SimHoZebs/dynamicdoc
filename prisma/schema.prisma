// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Doc {
  id         String     @id @default(uuid())
  title      String
  blockArray Block[]
  blockOrder String
	@@map("Page")
}

model Block {
  id      String    @id @default(uuid())
  type    String
  content String
  page    Doc   @relation(fields: [pageId], references: [id])
  pageId  String
}
